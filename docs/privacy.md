# Privacy

Public: market, store branch, product, size, price, unit price, date, freshness, status, anonymous evidence count, history.

Private: receipt photo, transaction number, payment data, user id, raw model output, exact GPS, abuse signals, device fingerprints.

Authorization uses `private.app_roles` and `market_memberships`, never `raw_user_meta_data`.
